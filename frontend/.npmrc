# React 19 + Next 15 ecosystem: some libraries still declare React 18 peer
# ranges. This keeps `npm install` (and the Docker build) from failing on
# harmless peer-dependency mismatches.
legacy-peer-deps=true
